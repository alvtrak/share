    def get_terminal_steps(self) -> List[str]:
        """Gets all the steps that have nothing depending on them.

        For example, given steps A <- B <- C:
            * "C" is a terminal step because no steps depend on it.

        Returns:
            All steps that have no dependents.
        """
        raise NotImplementedError("TODO")